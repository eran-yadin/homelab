kind="compose"
title="Headscale"
category="network"
desc="Self-hosted control server for Tailscale clients. Open source, no account needed."
homepage="https://headscale.net/"
ports="8080"
requires="docker"
stateful=1
needs_ram_mb=256
detect="container:headscale"
notes='The alternative to Tailscale-the-service: your machines still run the
  tailscale client, but the coordination server is this, on your own hardware.
  Edit files/config.yaml with your public address before starting.'
