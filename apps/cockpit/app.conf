kind="systemd"
unit="cockpit.socket"
adopted=1
title="Cockpit"
category="system"
desc="Browser console for the machine itself - logs, services, storage, updates."
homepage="https://cockpit-project.org/"
ports="9090"
stateful=0
detect="unit:cockpit.socket"
notes='Adopt-only, and socket-activated: the unit to control is cockpit.socket,
  not cockpit.service. Stopping the socket stops new sessions being accepted.'
