kind="script"
title="Tailscale"
category="network"
desc="Private mesh network. Reach this server from anywhere without opening a port."
homepage="https://tailscale.com/"
stateful=0
detect="bin:tailscale unit:tailscaled.service"
notes='Joining the network needs a browser login, so the last step is yours:
  sudo tailscale up. Set TS_AUTHKEY in the environment to skip that.
  For a fully self-hosted control server instead, install headscale.'
