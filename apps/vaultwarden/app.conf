kind="compose"
title="Vaultwarden"
category="security"
desc="Self-hosted Bitwarden-compatible password manager."
homepage="https://github.com/dani-garcia/vaultwarden"
ports="8222"
requires="docker"
stateful=1
needs_ram_mb=256
detect="container:vaultwarden"
notes='Signups are open only until you make your account -- set SIGNUPS_ALLOWED
  to false in the generated .env and restart. Browser extensions require HTTPS,
  so put this behind a reverse proxy before using it in anger.'
