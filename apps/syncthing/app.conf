kind="compose"
title="Syncthing"
category="files"
desc="Continuous file sync between your machines. No cloud in the middle."
homepage="https://syncthing.net/"
ports="8384"
requires="docker"
stateful=1
needs_ram_mb=256
detect="container:syncthing"
oneshot="init-perms"
notes='Sync traffic uses host networking so peers on the LAN are discovered
  properly; the web UI is on 8384.'
