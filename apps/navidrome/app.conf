kind="compose"
title="Navidrome"
category="media"
desc="Music server with a Subsonic API, so any Subsonic client can play from it."
homepage="https://www.navidrome.org/"
ports="4533"
requires="docker"
stateful=1
needs_ram_mb=256
detect="container:navidrome"
notes='Set MUSIC_DIR in the generated .env, then restart. Replaces the plain
  `docker run` navidrome on nucserver, which had no compose file at all.'
