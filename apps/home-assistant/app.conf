kind="compose"
title="Home Assistant"
category="home"
desc="Home automation. Talks to your devices, and to a local LLM if you add one."
homepage="https://www.home-assistant.io/"
ports="8123"
requires="docker"
stateful=1
needs_ram_mb=1024
needs_disk_mb=2048
detect="container:home-assistant"
notes='Host networking is required for device discovery (mDNS, SSDP), which
  does not cross a docker bridge.'
