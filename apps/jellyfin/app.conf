kind="compose"
title="Jellyfin"
category="media"
desc="Movies and TV. Uses hardware transcoding when the box has a GPU."
homepage="https://jellyfin.org/"
ports="8096"
requires="docker"
stateful=1
needs_ram_mb=1024
needs_disk_mb=2048
detect="container:jellyfin"
notes='Point MEDIA_DIR in the generated .env at your library, then restart.
  If /dev/dri exists the container gets it for QuickSync transcoding.'
