# web.stremio.com is a different origin to this server, so the browser refuses
# the request unless CORS checks are off. Set to 0 if you only ever use the
# native apps.
NO_CORS=1
STREMIO_HTTP_PORT=11470
STREMIO_HTTPS_PORT=12470
