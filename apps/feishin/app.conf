kind="container"
container="feishin"
adopted=1
title="Feishin"
category="media"
desc="Desktop-style web player for Navidrome and other Subsonic servers."
homepage="https://github.com/jeffvli/feishin"
ports="9180"
stateful=0
detect="container:feishin"
notes='Adopt-only: on nucserver this is a plain `docker run` container with no
  compose file, created before this catalog existed. It has no mounts, so
  there is nothing to back up. Its restart policy is "no", which is why it
  stayed down after the last reboot.'
