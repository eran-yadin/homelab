# Hostname suffix for per-app routes: paperless.nuc, cockpit.nuc, and so on.
# These only resolve once something answers DNS for them -- AdGuard, your
# router, or a line in each client's hosts file.
CADDY_DOMAIN=nuc
